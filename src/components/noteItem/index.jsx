// function NoteItem({ note }) {
//   return <li>{note.title}</li>;
// }

// export default NoteItem;

function NoteItem({ id, title, completed }) {
  return <li>{title}</li>;
}

export default NoteItem;
